// import { } from './actions'

export interface StateType {

}

const INITIAL_STATE: StateType = {
}

export default function reducer(state = INITIAL_STATE, action): StateType {
  switch (action.type) {
    default:
      return state
  }
}
